import {LoaderScreen as RNUILoaderScreen} from 'react-native-ui-lib'
import {legoRed} from '@/theme'

const LoaderScreen = () => <RNUILoaderScreen message="Loading..." loaderColor={legoRed} />

export default LoaderScreen
